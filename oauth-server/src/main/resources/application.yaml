## Servers
server:
  port: 18989

## Spring
spring:
  datasource:
    #username: sa
    #password:
    #url: jdbc:h2:file:./db/db-server/data;DB_CLOSE_ON_EXIT=FALSE
    #driver-class-name: org.h2.Driver
    #hikari:
    #  jdbc-url: jdbc:h2:file:./db/data;DB_CLOSE_ON_EXIT=FALSE
    url: jdbc:mysql://localhost:3306/testdb?useSSL=false&useUnicode=true&characterEncoding=utf8&seLegacyDatetimeCode=false
    username: root
    password: maria
    driver-class-name: org.mariadb.jdbc.Driver
    initialization-mode: always
    platform: oauth2
  jpa:
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.H2Dialect
        dialect: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: create-drop

## Application
app:
  admin-username: admin@email.com
  admin-password: admin
  user-username: user@gmail.com
  user-password: user
  client-id: application
  client-secret: pass
